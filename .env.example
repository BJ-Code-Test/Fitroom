# Kopie als .env.local anlegen und ausfuellen.
# Beide Werte sind oeffentlich (der Schutz kommt aus Row Level Security),
# gehoeren aber trotzdem nicht ins Repository.
VITE_SUPABASE_URL=https://<projekt-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon-key>
